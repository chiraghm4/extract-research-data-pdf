[project]
name = "pdf-parser"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi[standard]>=0.141.1",
    "openai[standard]>=3.8.0",
    "pdfplumber[standard]>=0.11.10",
]
[tool.fastapi]
entrypoint = "main:app"
